import Image from "next/image";
import { getPublicMediaUrl } from "@/features/media/utils/get-public-media-url";
import type { TimelineConfig } from "../schemas/timeline.schema";
type Media={id:string;path:string;alt:string|null;width:number|null;height:number|null};
export function InvitationTimeline({config,media}:{config:TimelineConfig;media:Media[]}){if(!config.items.length)return null;return <section className="w-full px-6 py-16"><div className="mx-auto max-w-4xl"><h2 className="mb-10 text-center text-4xl font-semibold">{config.title}</h2><div className="space-y-8">{config.items.map((item,i)=>{const m=item.mediaId?media.find(x=>x.id===item.mediaId):null;return <article key={i} className="grid gap-5 border-l-2 border-slate-300 pl-6 sm:grid-cols-[1fr_220px]"> <div><p className="text-sm uppercase tracking-widest text-slate-500">{item.date}</p><h3 className="mt-1 text-2xl font-semibold">{item.title}</h3>{item.description&&<p className="mt-2 leading-7 text-slate-600">{item.description}</p>}</div>{m&&<div className="relative aspect-square overflow-hidden rounded-xl"><Image src={getPublicMediaUrl(m.path)} alt={m.alt??""} fill sizes="220px" className="object-cover"/></div>}</article>})}</div></div></section>}
